name: trip_tuner
description: TripTuner - Travel Vibe Quiz App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  cloud_firestore: ^6.1.3
  provider: ^6.1.2
  google_maps_flutter: ^2.6.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/


